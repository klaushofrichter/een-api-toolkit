// Plain functions
export { getCurrentUser, getUsers, getUser } from './service'

// Composables
export { useCurrentUser, useUsers, useUser } from './composables'

// Composable option types
export type {
  UseCurrentUserOptions,
  UseUsersOptions,
  UseUserOptions
} from './composables'
